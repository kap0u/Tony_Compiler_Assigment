def main():
    x:=5;
    y=:=10;
    puts("Start");
    if x<y;
        puts("x is smaller");
    else:
        puts("y is smaller");
    end

    i:= 0;
    for i := 0; i<5; i:= i+1:
        puts("loop");
    end

    z:= (x+y)*2;

    puts("End");

end